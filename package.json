{
  "name": "dashboard-production-final",
  "version": "3.0.0",
  "private": true,
  "description": "Production dashboard with password-protected edit mode, backups, import/export and drag/drop editing.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check site/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {}
}
